# 📢 定时广告配置说明

## 功能概述

系统现在支持三种群发方式：

1. **`/bb 内容`** - 只发送给私信用户
2. **`/bb@ 内容`** - 发送给频道 + 群组 + 私信用户（全量群发）
3. **定时自动广告** - 按设定时间自动发送到所有渠道

---

## 配置方式

### 方法一：通过机器人命令配置（推荐）

#### 1. 查看当前配置
```
/getads
```

#### 2. 启用/禁用定时广告
```
/setads {"AUTO_ADS_ENABLED":true}
/setads {"AUTO_ADS_ENABLED":false}
```

#### 3. 设置发送时间（GMT+7 时区）
```
/setads {"AUTO_ADS_TIMES":["09:00","14:00","20:00"]}
```

#### 4. 设置广告内容
```
/setads {"AUTO_ADS_CONTENT":"🎉 六六ID贷 - 您的资金周转好帮手\n\n💰 高额度 · 💫低利息 · 🔒安全可靠"}
```

#### 5. 设置频道列表
```
/setads {"BROADCAST_CHANNELS":["@liuLiuid","@频道2"]}
```

#### 6. 设置群组列表
```
/setads {"BROADCAST_GROUPS":["-1001234567890","-1009876543210"]}
```

#### 7. 测试发送
```
/testads
```

---

### 方法二：直接修改代码（需要重新部署）

在 `worker.js` 文件的 `DEFAULT` 配置中修改：

```javascript
const DEFAULT = {
  // ... 其他配置 ...
  
  // 广播目标
  BROADCAST_CHANNELS: ["@liuLiuid"],  // 频道列表
  BROADCAST_GROUPS:   [],              // 群组列表
  
  // 定时广告配置
  AUTO_ADS_ENABLED: true,              // 是否启用
  AUTO_ADS_TIMES:   ["09:00", "14:00", "20:00"],  // 发送时间
  AUTO_ADS_CONTENT: "广告内容",
};
```

---

## 获取频道/群组 ID

### 频道 ID
- **公开频道**：使用 `@频道用户名`，例如 `@liuLiuid`
- **私有频道**：使用数字 ID，例如 `-1001234567890`

### 群组 ID
- **公开群组**：使用 `@群组用户名`
- **私有群组**：使用数字 ID，例如 `-1001234567890`

### 如何获取数字 ID？
1. 将机器人添加到频道/群组
2. 在频道/群组发送一条消息
3. 访问：`https://api.telegram.org/bot你的BOT_TOKEN/getUpdates`
4. 查找 `chat.id` 字段

---

## 定时任务设置

### Cloudflare Workers Cron 触发器

在 `wrangler.toml` 中配置：

```toml
[triggers]
crons = ["0 * * * *"]  # 每小时执行一次
```

或者在 Cloudflare Dashboard 中设置：
1. 进入 Workers & Pages
2. 选择你的 Worker
3. 点击 Triggers 标签
4. 添加 Cron Trigger
5. 设置为 `0 * * * *`（每小时）或 `*/30 * * * *`（每30分钟）

**注意**：定时任务的精度取决于 Cron 触发频率。建议设置为每小时或每30分钟执行一次。

---

## 完整配置示例

```bash
# 1. 启用定时广告
/setads {"AUTO_ADS_ENABLED":true}

# 2. 设置每天3次发送（早中晚）
/setads {"AUTO_ADS_TIMES":["09:00","14:00","20:00"]}

# 3. 设置广告内容
/setads {"AUTO_ADS_CONTENT":"🎉 六六ID贷款助手\n\n💰 高额度 低利息\n⚡ 5分钟到账\n🔒 安全可靠\n\n立即申请：@liuliuidid_bot"}

# 4. 设置频道
/setads {"BROADCAST_CHANNELS":["@liuLiuid"]}

# 5. 测试发送
/testads
```

---

## 注意事项

1. **机器人权限**：
   - 频道：机器人需要是频道管理员，且有发送消息权限
   - 群组：机器人需要在群组中，且有发送消息权限

2. **时间精度**：
   - 定时任务依赖 Cloudflare Cron 触发频率
   - 允许 ±1 分钟的误差
   - 建议 Cron 设置为每小时或每30分钟

3. **发送频率**：
   - 避免设置过于频繁的发送时间
   - 建议每天不超过 3-5 次

4. **内容格式**：
   - 支持 HTML 格式：`<b>粗体</b>`、`<code>代码</code>`
   - 支持换行：使用 `\n`
   - 支持链接：`<a href="url">文字</a>`

---

## 故障排查

### 广告没有发送？
1. 检查是否启用：`/getads`
2. 检查 Cron 触发器是否配置
3. 查看 Cloudflare Workers 日志

### 发送失败？
1. 检查机器人是否在频道/群组中
2. 检查机器人权限
3. 检查频道/群组 ID 是否正确

### 时间不准确？
1. 检查时区设置（代码使用 GMT+7）
2. 调整 Cron 触发频率
3. 查看实际执行时间日志
